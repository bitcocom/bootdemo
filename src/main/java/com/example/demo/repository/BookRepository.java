package com.example.demo.repository;

import org.springframework.data.jpa.repository.JpaRepository;

// JPA
public interface BookRepository extends JpaRepository { // JpaRepository->CRUD Method 제공 + 직접만들수도있다(규칙)




}
